{
  "name": "global-quant-watch",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "description": "A local multi-market quantitative research workspace for ASX, US stocks, and China A-shares.",
  "scripts": {
    "start": "node server.mjs",
    "check": "node --check server.mjs && node --check app.js && SERVER_DISABLE_LISTEN=true node --test tests/test_server.mjs && python3 -m unittest discover -s tests -v",
    "check:node": "SERVER_DISABLE_LISTEN=true node --test tests/test_server.mjs",
    "check:python": "python3 -m unittest discover -s tests -v",
    "check:providers": "node tools/check-providers.mjs",
    "import:postman-env": "node tools/import-postman-env.mjs"
  },
  "keywords": [
    "stocks",
    "asx",
    "us-stocks",
    "a-shares",
    "quant",
    "portfolio",
    "technical-analysis"
  ],
  "author": "",
  "license": "MIT"
}
